apiVersion: batch/v1
kind: CronJob
metadata:
  name: scheduler
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: scheduler
              image: myorg/scheduler:latest
          restartPolicy: OnFailure
