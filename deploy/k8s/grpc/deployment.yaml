apiVersion: apps/v1
kind: Deployment
metadata:
  name: template-grpc
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: worker
          image: template-app:latest
          command: ["python", "-m", "template_app.cli", "grpc"]
