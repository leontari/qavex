frontend (Vue or Svelte + lightweight-charts)

objective:
  - UI
  - charts
  - routing
  - authorisation
  - settings
  
interface:
  - interacts only with ```backend-api```

layers:
 - lightweght-charts -> candles
 - overlays -> indicators
 - markers -> signals
